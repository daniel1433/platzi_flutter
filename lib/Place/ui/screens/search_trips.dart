import 'package:flutter/material.dart';

class SearchTrips extends StatelessWidget {
  const SearchTrips({Key? key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.greenAccent,
    );
  }
}
